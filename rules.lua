rules = {}
rules.scale = 2
rules.camera = {}
rules.camera.Speed = 0
rules.camera.MinScale = 1
rules.camera.MaxScale = 1
rules.camera.ScrollRatioLimit = 0

rules.realTileDim = 0.5 -- une tuile représente 0.5m
rules.tileWidth = 16

rules.printTime = 2

return rules